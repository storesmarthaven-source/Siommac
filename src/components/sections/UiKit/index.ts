/**
 * src/components/sections/UiKit — barrel.
 *
 * Importing this registers the UI Kit module (see main.tsx).
 */

export { uiKitModule } from './module';
export { mountUiKitSection, unmountUiKitSection } from './mount';
