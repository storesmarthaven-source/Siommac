/**
 * src/components/sections/AdminDashboard/index.ts
 *
 * Public exports for the AdminDashboard section.
 *
 * AdminStatCards  — six KPI tiles, rendered inside .dash-stats-row
 * AdminRecentTable — tbody rows for the recent attendance table
 *
 * Both are slotted directly into AdminSections.tsx — no imperative mount needed.
 */

export { AdminStatCards, AdminRecentTable } from './AdminDashboardController';
