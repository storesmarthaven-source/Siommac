{
  "compilerOptions": {
    /* Language */
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    /* JSX — Preact */
    "jsx": "react-jsx",
    "jsxImportSource": "preact",

    /* Strictness */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,

    /* Output — Vite handles emit; tsc only type-checks */
    "noEmit": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,

    /* Path aliases — must mirror vite.config.ts */
    "baseUrl": ".",
    "paths": {
      /* React-typed libraries (react-grid-layout, react-draggable/-resizable) resolve their
         `import 'react'` types to preact/compat, mirroring the runtime alias in vite.config.ts —
         so their JSX/context/ReactNode types unify with Preact's instead of clashing. */
      "react":       ["./node_modules/preact/compat/src/index.d.ts"],
      "react-dom":   ["./node_modules/preact/compat/src/index.d.ts"],
      "@/*":         ["src/*"],
      "@store":      ["src/store/index.ts"],
      "@store/*":    ["src/store/*"],
      "@lib":        ["src/lib/index.ts"],
      "@lib/*":      ["src/lib/*"],
      "@shared/*":   ["src/components/shared/*"],
      "@sections/*": ["src/components/sections/*"],
      "@payslip/*":  ["src/components/sections/PayslipStudio/*"],
      "@ui":         ["src/ui/index.ts"],
      "@ui/*":       ["src/ui/*"],
      "@cfg":           ["src/config/index.ts"],
      "@cfg/*":         ["src/config/*"],
      "@components/*":  ["src/components/*"],
      "@shell":         ["src/shell/index.ts"],
      "@shell/*":       ["src/shell/*"],
      "@api":           ["src/api/index.ts"],
      "@api/*":         ["src/api/*"]
    },

    /* Emit helpers inline (no tslib dep needed) */
    "importHelpers": false,
    "skipLibCheck": true
  },

  "include": ["src/**/*", "types/**/*"],
  "exclude": ["node_modules", "dist", "netlify"]
}
